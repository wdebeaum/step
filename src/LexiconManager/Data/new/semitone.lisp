;;;;
;;;; W::SEMITONE
;;;;

(define-words :pos W::n :templ COUNT-PRED-TEMPL
 :words (
  ;; the musical interval between adjacent keys on a keyboard instrument; the smallest interval used in classical Western music, equal to a twelfth of an octave or half a tone; a half step.
  (W::SEMITONE
   (SENSES
    ((LF-PARENT ONT::HALF-STEP)
     (example "A semitone is the smallest interval in Western music.")
     (meta-data :wn ("semitone%1:10:00" "half_step%1:10:00"))
     )
    )
   )
))
