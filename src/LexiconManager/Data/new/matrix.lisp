;;;;
;;;; w::matrix
;;;;

(define-words :pos W::n :templ COUNT-PRED-TEMPL
 :words (
   (w::matrix
   (senses
    ((meta-data :origin plow :entry-date 20060615 :change-date nil :wn ("matrix%1:14:00") :comment pq)
     (LF-PARENT ONT::data-structure) ;; like array
     (EXAMPLE "The size of a matrix is defined by the number of rows and columns that it contains")
     )
    )
   )
))

